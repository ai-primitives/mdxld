---
$context: schema.org
$type: BusStop
title: BusStop
description: A bus stop.
properties:

subClasses:

parentClass: CivicStructure
---

# BusStop

A bus stop.

## Type Hierarchy

BusStop is a type of CivicStructure.

### Direct Subtypes

No types directly inherit from BusStop.

## Properties

BusStop defines the following properties in addition to those inherited from CivicStructure:



## Usage Example

```typescript
interface BusStop {
  $context?: 'schema.org'
  $type: 'BusStop'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "BusStop",
  "name": "Example BusStop",
  "description": "An example of the BusStop type",
  "url": "https://example.com/busstop",
  "identifier": "example-busstop-1"
}
```

## Notes

- BusStop inherits all properties from CivicStructure
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
