---
$context: schema.org
$type: AppendAction
title: AppendAction
description: The act of inserting at the end if an ordered collection.
properties:

subClasses:

parentClass: InsertAction
---

# AppendAction

The act of inserting at the end if an ordered collection.

## Type Hierarchy

AppendAction is a type of InsertAction.

### Direct Subtypes

No types directly inherit from AppendAction.

## Properties

AppendAction defines the following properties in addition to those inherited from InsertAction:



## Usage Example

```typescript
interface AppendAction {
  $context?: 'schema.org'
  $type: 'AppendAction'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "AppendAction",
  "name": "Example AppendAction",
  "description": "An example of the AppendAction type",
  "url": "https://example.com/appendaction",
  "identifier": "example-appendaction-1"
}
```

## Notes

- AppendAction inherits all properties from InsertAction
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
