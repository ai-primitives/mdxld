---
$context: schema.org
$type: PlayAction
title: PlayAction
description: The act of playing/exercising/training/performing for enjoyment, leisure, recreation, competition or exercise.\n\nRelated actions:\n\n* [[ListenAction]]: Unlike ListenAction (which is under ConsumeAction), PlayAction refers to performing for an audience or at an event, rather than consuming music.\n* [[WatchAction]]: Unlike WatchAction (which is under ConsumeAction), PlayAction refers to showing/displaying for an audience or at an event, rather than consuming visual content.
properties:

subClasses:
  - ExerciseAction
  - PerformAction
parentClass: Action
---

# PlayAction

The act of playing/exercising/training/performing for enjoyment, leisure, recreation, competition or exercise.\n\nRelated actions:\n\n* [[ListenAction]]: Unlike ListenAction (which is under ConsumeAction), PlayAction refers to performing for an audience or at an event, rather than consuming music.\n* [[WatchAction]]: Unlike WatchAction (which is under ConsumeAction), PlayAction refers to showing/displaying for an audience or at an event, rather than consuming visual content.

## Type Hierarchy

PlayAction is a type of Action.

### Direct Subtypes

The following types directly inherit from PlayAction:

- ExerciseAction: The act of participating in exertive activity for the purposes of improving health and fitness.
- PerformAction: The act of participating in performance arts.

## Properties

PlayAction defines the following properties in addition to those inherited from Action:



## Usage Example

```typescript
interface PlayAction {
  $context?: 'schema.org'
  $type: 'PlayAction'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "PlayAction",
  "name": "Example PlayAction",
  "description": "An example of the PlayAction type",
  "url": "https://example.com/playaction",
  "identifier": "example-playaction-1"
}
```

## Notes

- PlayAction inherits all properties from Action
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
