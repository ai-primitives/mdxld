---
$context: schema.org
$type: TaxiService
title: TaxiService
description: A service for a vehicle for hire with a driver for local travel. Fares are usually calculated based on distance traveled.
properties:

subClasses:

parentClass: Service
---

# TaxiService

A service for a vehicle for hire with a driver for local travel. Fares are usually calculated based on distance traveled.

## Type Hierarchy

TaxiService is a type of Service.

### Direct Subtypes

No types directly inherit from TaxiService.

## Properties

TaxiService defines the following properties in addition to those inherited from Service:



## Usage Example

```typescript
interface TaxiService {
  $context?: 'schema.org'
  $type: 'TaxiService'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "TaxiService",
  "name": "Example TaxiService",
  "description": "An example of the TaxiService type",
  "url": "https://example.com/taxiservice",
  "identifier": "example-taxiservice-1"
}
```

## Notes

- TaxiService inherits all properties from Service
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
