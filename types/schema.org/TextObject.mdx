---
$context: schema.org
$type: TextObject
title: TextObject
description: A text file. The text can be unformatted or contain markup, html, etc.
properties:

subClasses:

parentClass: MediaObject
---

# TextObject

A text file. The text can be unformatted or contain markup, html, etc.

## Type Hierarchy

TextObject is a type of MediaObject.

### Direct Subtypes

No types directly inherit from TextObject.

## Properties

TextObject defines the following properties in addition to those inherited from MediaObject:



## Usage Example

```typescript
interface TextObject {
  $context?: 'schema.org'
  $type: 'TextObject'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "TextObject",
  "name": "Example TextObject",
  "description": "An example of the TextObject type",
  "url": "https://example.com/textobject",
  "identifier": "example-textobject-1"
}
```

## Notes

- TextObject inherits all properties from MediaObject
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
