---
$context: schema.org
$type: ScheduleAction
title: ScheduleAction
description: Scheduling future actions, events, or tasks.\n\nRelated actions:\n\n* [[ReserveAction]]: Unlike ReserveAction, ScheduleAction allocates future actions (e.g. an event, a task, etc) towards a time slot / spatial allocation.
properties:

subClasses:

parentClass: PlanAction
---

# ScheduleAction

Scheduling future actions, events, or tasks.\n\nRelated actions:\n\n* [[ReserveAction]]: Unlike ReserveAction, ScheduleAction allocates future actions (e.g. an event, a task, etc) towards a time slot / spatial allocation.

## Type Hierarchy

ScheduleAction is a type of PlanAction.

### Direct Subtypes

No types directly inherit from ScheduleAction.

## Properties

ScheduleAction defines the following properties in addition to those inherited from PlanAction:



## Usage Example

```typescript
interface ScheduleAction {
  $context?: 'schema.org'
  $type: 'ScheduleAction'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "ScheduleAction",
  "name": "Example ScheduleAction",
  "description": "An example of the ScheduleAction type",
  "url": "https://example.com/scheduleaction",
  "identifier": "example-scheduleaction-1"
}
```

## Notes

- ScheduleAction inherits all properties from PlanAction
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
