---
$context: schema.org
$type: CompoundPriceSpecification
title: CompoundPriceSpecification
description: A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. "electricity" or "final cleaning").
properties:
  - priceComponent
subClasses:

parentClass: PriceSpecification
---

# CompoundPriceSpecification

A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. "electricity" or "final cleaning").

## Type Hierarchy

CompoundPriceSpecification is a type of PriceSpecification.

### Direct Subtypes

No types directly inherit from CompoundPriceSpecification.

## Properties

CompoundPriceSpecification defines the following properties in addition to those inherited from PriceSpecification:

- priceComponent: This property links to all [[UnitPriceSpecification]] nodes that apply in parallel for the [[CompoundPriceSpecification]] node.

## Usage Example

```typescript
interface CompoundPriceSpecification {
  $context?: 'schema.org'
  $type: 'CompoundPriceSpecification'
  priceComponent?: UnitPriceSpecification
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "CompoundPriceSpecification",
  "name": "Example CompoundPriceSpecification",
  "description": "An example of the CompoundPriceSpecification type",
  "url": "https://example.com/compoundpricespecification",
  "identifier": "example-compoundpricespecification-1"
}
```

## Notes

- CompoundPriceSpecification inherits all properties from PriceSpecification
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
