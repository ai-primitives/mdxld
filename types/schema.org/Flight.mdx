---
$context: schema.org
$type: Flight
title: Flight
description: An airline flight.
properties:
  direct:
    - name: boardingPolicy
      type: BoardingPolicyType
      description: The type of boarding policy used by the airline (e.g. zone-based or group-based).
    - name: arrivalTerminal
      type: string
      description: Identifier of the flight's arrival terminal.
    - name: departureGate
      type: string
      description: Identifier of the flight's departure gate.
    - name: departureAirport
      type: Airport
      description: The airport where the flight originates.
    - name: carrier
      type: Organization
      description: 'carrier' is an out-dated term indicating the 'provider' for parcel delivery and flights.
    - name: aircraft
      type: Vehicle | string
      description: The kind of aircraft (e.g., "Boeing 747").
    - name: seller
      type: Organization | Person
      description: An entity which offers (sells / leases / lends / loans) the services / goods.  A seller may also be a provider.
    - name: arrivalGate
      type: string
      description: Identifier of the flight's arrival gate.
    - name: webCheckinTime
      type: string
      description: The time when a passenger can check into the flight online.
    - name: estimatedFlightDuration
      type: Duration | string
      description: The estimated time the flight will take.
    - name: flightDistance
      type: Distance | string
      description: The distance of the flight.
    - name: mealService
      type: string
      description: Description of the meals that will be provided or available for purchase.
    - name: departureTerminal
      type: string
      description: Identifier of the flight's departure terminal.
    - name: arrivalAirport
      type: Airport
      description: The airport where the flight terminates.
    - name: flightNumber
      type: string
      description: The unique identifier for a flight including the airline IATA code. For example, if describing United flight 110, where the IATA code for United is 'UA', the flightNumber is 'UA110'.
  inherited:
    - name: departureTime
      type: string
      description: The expected departure time.
      inheritedFrom: Trip
    - name: partOfTrip
      type: Trip
      description: Identifies that this [[Trip]] is a subTrip of another Trip.  For example Day 1, Day 2, etc. of a multi-day trip.
      inheritedFrom: Trip
    - name: tripOrigin
      type: Place
      description: The location of origin of the trip, prior to any destination(s).
      inheritedFrom: Trip
    - name: itinerary
      type: ItemList | Place
      description: Destination(s) ( [[Place]] ) that make up a trip. For a trip where destination order is important use [[ItemList]] to specify that order (see examples).
      inheritedFrom: Trip
    - name: arrivalTime
      type: string
      description: The expected arrival time.
      inheritedFrom: Trip
    - name: offers
      type: Demand | Offer
      description: An offer to provide this item&#x2014;for example, an offer to sell a product, rent the DVD of a movie, perform a service, or give away tickets to an event. Use [[businessFunction]] to indicate the kind of transaction offered, i.e. sell, lease, etc. This property can also be used to describe a [[Demand]]. While this property is listed as expected on a number of common types, it can be used in others. In that case, using a second type, such as Product or a subtype of Product, can clarify the nature of the offer.
      
      inheritedFrom: Trip
    - name: provider
      type: Organization | Person
      description: The service provider, service operator, or service performer; the goods producer. Another party (a seller) may offer those services or goods on behalf of the provider. A provider may also serve as the seller.
      inheritedFrom: Trip
    - name: subTrip
      type: Trip
      description: Identifies a [[Trip]] that is a subTrip of this Trip.  For example Day 1, Day 2, etc. of a multi-day trip.
      inheritedFrom: Trip
    - name: name
      type: string
      description: The name of the item.
      inheritedFrom: Thing
    - name: disambiguatingDescription
      type: string
      description: A sub property of description. A short description of the item used to disambiguate from other, similar items. Information from other properties (in particular, name) may be necessary for the description to be useful for disambiguation.
      inheritedFrom: Thing
    - name: potentialAction
      type: Action
      description: Indicates a potential Action, which describes an idealized action in which this thing would play an 'object' role.
      inheritedFrom: Thing
    - name: url
      type: string
      description: URL of the item.
      inheritedFrom: Thing
    - name: identifier
      type: PropertyValue | string
      description: The identifier property represents any kind of identifier for any kind of [[Thing]], such as ISBNs, GTIN codes, UUIDs etc. Schema.org provides dedicated properties for representing many of these, either as textual strings or as URL (URI) links. See [background notes](/docs/datamodel.html#identifierBg) for more details.
        
      inheritedFrom: Thing
    - name: subjectOf
      type: CreativeWork | Event
      description: A CreativeWork or Event about this Thing.
      inheritedFrom: Thing
    - name: description
      type: string
      description: A description of the item.
      inheritedFrom: Thing
    - name: additionalType
      type: string
      description: An additional type for the item, typically used for adding more specific types from external vocabularies in microdata syntax. This is a relationship between something and a class that the thing is in. Typically the value is a URI-identified RDF class, and in this case corresponds to the
    use of rdf:type in RDF. Text values can be used sparingly, for cases where useful information can be added without their being an appropriate schema to reference. In the case of text values, the class label should follow the schema.org <a href="https://schema.org/docs/styleguide.html">style guide</a>.
      inheritedFrom: Thing
    - name: image
      type: ImageObject | string
      description: An image of the item. This can be a [[URL]] or a fully described [[ImageObject]].
      inheritedFrom: Thing
    - name: sameAs
      type: string
      description: URL of a reference Web page that unambiguously indicates the item's identity. E.g. the URL of the item's Wikipedia page, Wikidata entry, or official website.
      inheritedFrom: Thing
    - name: alternateName
      type: string
      description: An alias for the item.
      inheritedFrom: Thing
    - name: mainEntityOfPage
      type: CreativeWork | string
      description: Indicates a page (or other CreativeWork) for which this thing is the main entity being described. See [background notes](/docs/datamodel.html#mainEntityBackground) for details.
      inheritedFrom: Thing
parentClass: Trip
---
# Flight

An airline flight.

## Type Hierarchy

Flight is a type of Trip.

### Direct Subtypes

No types directly inherit from Flight.

## Properties

### Direct Properties

- boardingPolicy: The type of boarding policy used by the airline (e.g. zone-based or group-based).
- arrivalTerminal: Identifier of the flight's arrival terminal.
- departureGate: Identifier of the flight's departure gate.
- departureAirport: The airport where the flight originates.
- carrier: 'carrier' is an out-dated term indicating the 'provider' for parcel delivery and flights.
- aircraft: The kind of aircraft (e.g., "Boeing 747").
- seller: An entity which offers (sells / leases / lends / loans) the services / goods.  A seller may also be a provider.
- arrivalGate: Identifier of the flight's arrival gate.
- webCheckinTime: The time when a passenger can check into the flight online.
- estimatedFlightDuration: The estimated time the flight will take.
- flightDistance: The distance of the flight.
- mealService: Description of the meals that will be provided or available for purchase.
- departureTerminal: Identifier of the flight's departure terminal.
- arrivalAirport: The airport where the flight terminates.
- flightNumber: The unique identifier for a flight including the airline IATA code. For example, if describing United flight 110, where the IATA code for United is 'UA', the flightNumber is 'UA110'.

### Inherited Properties

- departureTime: The expected departure time. (inherited from Trip)
- partOfTrip: Identifies that this [[Trip]] is a subTrip of another Trip.  For example Day 1, Day 2, etc. of a multi-day trip. (inherited from Trip)
- tripOrigin: The location of origin of the trip, prior to any destination(s). (inherited from Trip)
- itinerary: Destination(s) ( [[Place]] ) that make up a trip. For a trip where destination order is important use [[ItemList]] to specify that order (see examples). (inherited from Trip)
- arrivalTime: The expected arrival time. (inherited from Trip)
- offers: An offer to provide this item&#x2014;for example, an offer to sell a product, rent the DVD of a movie, perform a service, or give away tickets to an event. Use [[businessFunction]] to indicate the kind of transaction offered, i.e. sell, lease, etc. This property can also be used to describe a [[Demand]]. While this property is listed as expected on a number of common types, it can be used in others. In that case, using a second type, such as Product or a subtype of Product, can clarify the nature of the offer.
       (inherited from Trip)
- provider: The service provider, service operator, or service performer; the goods producer. Another party (a seller) may offer those services or goods on behalf of the provider. A provider may also serve as the seller. (inherited from Trip)
- subTrip: Identifies a [[Trip]] that is a subTrip of this Trip.  For example Day 1, Day 2, etc. of a multi-day trip. (inherited from Trip)
- name: The name of the item. (inherited from Thing)
- disambiguatingDescription: A sub property of description. A short description of the item used to disambiguate from other, similar items. Information from other properties (in particular, name) may be necessary for the description to be useful for disambiguation. (inherited from Thing)
- potentialAction: Indicates a potential Action, which describes an idealized action in which this thing would play an 'object' role. (inherited from Thing)
- url: URL of the item. (inherited from Thing)
- identifier: The identifier property represents any kind of identifier for any kind of [[Thing]], such as ISBNs, GTIN codes, UUIDs etc. Schema.org provides dedicated properties for representing many of these, either as textual strings or as URL (URI) links. See [background notes](/docs/datamodel.html#identifierBg) for more details.
         (inherited from Thing)
- subjectOf: A CreativeWork or Event about this Thing. (inherited from Thing)
- description: A description of the item. (inherited from Thing)
- additionalType: An additional type for the item, typically used for adding more specific types from external vocabularies in microdata syntax. This is a relationship between something and a class that the thing is in. Typically the value is a URI-identified RDF class, and in this case corresponds to the
    use of rdf:type in RDF. Text values can be used sparingly, for cases where useful information can be added without their being an appropriate schema to reference. In the case of text values, the class label should follow the schema.org <a href="https://schema.org/docs/styleguide.html">style guide</a>. (inherited from Thing)
- image: An image of the item. This can be a [[URL]] or a fully described [[ImageObject]]. (inherited from Thing)
- sameAs: URL of a reference Web page that unambiguously indicates the item's identity. E.g. the URL of the item's Wikipedia page, Wikidata entry, or official website. (inherited from Thing)
- alternateName: An alias for the item. (inherited from Thing)
- mainEntityOfPage: Indicates a page (or other CreativeWork) for which this thing is the main entity being described. See [background notes](/docs/datamodel.html#mainEntityBackground) for details. (inherited from Thing)

## Usage Example

```typescript
interface Flight extends Trip {
  $context?: 'schema.org'
  $type: 'Flight'
  boardingPolicy?: BoardingPolicyType
  arrivalTerminal?: string
  departureGate?: string
  departureAirport?: Airport
  carrier?: Organization
  aircraft?: Vehicle | string
  seller?: Organization | Person
  arrivalGate?: string
  webCheckinTime?: string
  estimatedFlightDuration?: Duration | string
  flightDistance?: Distance | string
  mealService?: string
  departureTerminal?: string
  arrivalAirport?: Airport
  flightNumber?: string
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "Flight",
  "name": "Example Flight",
  "description": "An example of the Flight type",
  "url": "https://example.com/flight",
  "identifier": "example-flight-1"
}
```

## Notes

- Flight inherits all properties from Trip
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface