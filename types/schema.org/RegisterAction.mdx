---
$context: schema.org
$type: RegisterAction
title: RegisterAction
description: The act of registering to be a user of a service, product or web page.\n\nRelated actions:\n\n* [[JoinAction]]: Unlike JoinAction, RegisterAction implies you are registering to be a user of a service, *not* a group/team of people.\n* [[FollowAction]]: Unlike FollowAction, RegisterAction doesn't imply that the agent is expecting to poll for updates from the object.\n* [[SubscribeAction]]: Unlike SubscribeAction, RegisterAction doesn't imply that the agent is expecting updates from the object.
properties:

subClasses:

parentClass: InteractAction
---

# RegisterAction

The act of registering to be a user of a service, product or web page.\n\nRelated actions:\n\n* [[JoinAction]]: Unlike JoinAction, RegisterAction implies you are registering to be a user of a service, *not* a group/team of people.\n* [[FollowAction]]: Unlike FollowAction, RegisterAction doesn't imply that the agent is expecting to poll for updates from the object.\n* [[SubscribeAction]]: Unlike SubscribeAction, RegisterAction doesn't imply that the agent is expecting updates from the object.

## Type Hierarchy

RegisterAction is a type of InteractAction.

### Direct Subtypes

No types directly inherit from RegisterAction.

## Properties

RegisterAction defines the following properties in addition to those inherited from InteractAction:



## Usage Example

```typescript
interface RegisterAction {
  $context?: 'schema.org'
  $type: 'RegisterAction'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "RegisterAction",
  "name": "Example RegisterAction",
  "description": "An example of the RegisterAction type",
  "url": "https://example.com/registeraction",
  "identifier": "example-registeraction-1"
}
```

## Notes

- RegisterAction inherits all properties from InteractAction
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
