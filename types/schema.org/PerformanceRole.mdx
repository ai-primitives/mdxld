---
$context: schema.org
$type: PerformanceRole
title: PerformanceRole
description: A PerformanceRole is a Role that some entity places with regard to a theatrical performance, e.g. in a Movie, TVSeries etc.
properties:
  - characterName
subClasses:

parentClass: Role
---

# PerformanceRole

A PerformanceRole is a Role that some entity places with regard to a theatrical performance, e.g. in a Movie, TVSeries etc.

## Type Hierarchy

PerformanceRole is a type of Role.

### Direct Subtypes

No types directly inherit from PerformanceRole.

## Properties

PerformanceRole defines the following properties in addition to those inherited from Role:

- characterName: The name of a character played in some acting or performing role, i.e. in a PerformanceRole.

## Usage Example

```typescript
interface PerformanceRole {
  $context?: 'schema.org'
  $type: 'PerformanceRole'
  characterName?: string
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "PerformanceRole",
  "name": "Example PerformanceRole",
  "description": "An example of the PerformanceRole type",
  "url": "https://example.com/performancerole",
  "identifier": "example-performancerole-1"
}
```

## Notes

- PerformanceRole inherits all properties from Role
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
