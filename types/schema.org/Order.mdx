---
$context: schema.org
$type: Order
title: Order
description: An order is a confirmation of a transaction (a receipt), which can contain multiple line items, each represented by an Offer that has been accepted by the customer.
properties:
  - acceptedOffer
  - billingAddress
  - discount
  - discountCode
  - discountCurrency
  - isGift
  - merchant
  - orderDate
  - orderNumber
  - orderStatus
  - partOfInvoice
  - paymentUrl
subClasses:

parentClass: Intangible
---

# Order

An order is a confirmation of a transaction (a receipt), which can contain multiple line items, each represented by an Offer that has been accepted by the customer.

## Type Hierarchy

Order is a type of Intangible.

### Direct Subtypes

No types directly inherit from Order.

## Properties

Order defines the following properties in addition to those inherited from Intangible:

- acceptedOffer: The offer(s) -- e.g., product, quantity and price combinations -- included in the order.
- billingAddress: The billing address for the order.
- discount: Any discount applied (to an Order).
- discountCode: Code used to redeem a discount.
- discountCurrency: The currency of the discount.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR".
- isGift: Indicates whether the offer was accepted as a gift for someone other than the buyer.
- merchant: 'merchant' is an out-dated term for 'seller'.
- orderDate: Date order was placed.
- orderNumber: The identifier of the transaction.
- orderStatus: The current status of the order.
- partOfInvoice: The order is being paid as part of the referenced Invoice.
- paymentUrl: The URL for sending a payment.

## Usage Example

```typescript
interface Order {
  $context?: 'schema.org'
  $type: 'Order'
  acceptedOffer?: Offer
  billingAddress?: PostalAddress
  discount?: number | string
  discountCode?: string
  discountCurrency?: string
  isGift?: boolean
  merchant?: Organization | Person
  orderDate?: string
  orderNumber?: string
  orderStatus?: OrderStatus
  partOfInvoice?: Invoice
  paymentUrl?: string
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "Order",
  "name": "Example Order",
  "description": "An example of the Order type",
  "url": "https://example.com/order",
  "identifier": "example-order-1"
}
```

## Notes

- Order inherits all properties from Intangible
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
