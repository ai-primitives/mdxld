---
$context: schema.org
$type: AudioObject
title: AudioObject
description: An audio file.
properties:

subClasses:
  - AudioObjectSnapshot
parentClass: MediaObject
---

# AudioObject

An audio file.

## Type Hierarchy

AudioObject is a type of MediaObject.

### Direct Subtypes

The following types directly inherit from AudioObject:

- AudioObjectSnapshot: A specific and exact (byte-for-byte) version of an [[AudioObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity.

## Properties

AudioObject defines the following properties in addition to those inherited from MediaObject:



## Usage Example

```typescript
interface AudioObject {
  $context?: 'schema.org'
  $type: 'AudioObject'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "AudioObject",
  "name": "Example AudioObject",
  "description": "An example of the AudioObject type",
  "url": "https://example.com/audioobject",
  "identifier": "example-audioobject-1"
}
```

## Notes

- AudioObject inherits all properties from MediaObject
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
