---
$context: schema.org
$type: MedicalProcedure
title: MedicalProcedure
description: A process of care used in either a diagnostic, therapeutic, preventive or palliative capacity that relies on invasive (surgical), non-invasive, or other techniques.
properties:
  - followup
  - howPerformed
  - preparation
  - procedureType
subClasses:
  - DiagnosticProcedure
  - SurgicalProcedure
  - TherapeuticProcedure
parentClass: MedicalEntity
---

# MedicalProcedure

A process of care used in either a diagnostic, therapeutic, preventive or palliative capacity that relies on invasive (surgical), non-invasive, or other techniques.

## Type Hierarchy

MedicalProcedure is a type of MedicalEntity.

### Direct Subtypes

The following types directly inherit from MedicalProcedure:

- DiagnosticProcedure: A medical procedure intended primarily for diagnostic, as opposed to therapeutic, purposes.
- SurgicalProcedure: A medical procedure involving an incision with instruments; performed for diagnose, or therapeutic purposes.
- TherapeuticProcedure: A medical procedure intended primarily for therapeutic purposes, aimed at improving a health condition.

## Properties

MedicalProcedure defines the following properties in addition to those inherited from MedicalEntity:

- followup: Typical or recommended followup care after the procedure is performed.
- howPerformed: How the procedure is performed.
- preparation: Typical preparation that a patient must undergo before having the procedure performed.
- procedureType: The type of procedure, for example Surgical, Noninvasive, or Percutaneous.

## Usage Example

```typescript
interface MedicalProcedure {
  $context?: 'schema.org'
  $type: 'MedicalProcedure'
  followup?: string
  howPerformed?: string
  preparation?: MedicalEntity | string
  procedureType?: MedicalProcedureType
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "MedicalProcedure",
  "name": "Example MedicalProcedure",
  "description": "An example of the MedicalProcedure type",
  "url": "https://example.com/medicalprocedure",
  "identifier": "example-medicalprocedure-1"
}
```

## Notes

- MedicalProcedure inherits all properties from MedicalEntity
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
