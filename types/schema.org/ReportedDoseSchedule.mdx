---
$context: schema.org
$type: ReportedDoseSchedule
title: ReportedDoseSchedule
description: A patient-reported or observed dosing schedule for a drug or supplement.
properties:

subClasses:

parentClass: DoseSchedule
---

# ReportedDoseSchedule

A patient-reported or observed dosing schedule for a drug or supplement.

## Type Hierarchy

ReportedDoseSchedule is a type of DoseSchedule.

### Direct Subtypes

No types directly inherit from ReportedDoseSchedule.

## Properties

ReportedDoseSchedule defines the following properties in addition to those inherited from DoseSchedule:



## Usage Example

```typescript
interface ReportedDoseSchedule {
  $context?: 'schema.org'
  $type: 'ReportedDoseSchedule'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "ReportedDoseSchedule",
  "name": "Example ReportedDoseSchedule",
  "description": "An example of the ReportedDoseSchedule type",
  "url": "https://example.com/reporteddoseschedule",
  "identifier": "example-reporteddoseschedule-1"
}
```

## Notes

- ReportedDoseSchedule inherits all properties from DoseSchedule
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
