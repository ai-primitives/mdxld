---
$context: schema.org
$type: BankAccount
title: BankAccount
description: A product or service offered by a bank whereby one may deposit, withdraw or transfer money and in some cases be paid interest.
properties:
  - accountMinimumInflow
  - accountOverdraftLimit
  - bankAccountType
subClasses:

parentClass: FinancialProduct
---

# BankAccount

A product or service offered by a bank whereby one may deposit, withdraw or transfer money and in some cases be paid interest.

## Type Hierarchy

BankAccount is a type of FinancialProduct.

### Direct Subtypes

No types directly inherit from BankAccount.

## Properties

BankAccount defines the following properties in addition to those inherited from FinancialProduct:

- accountMinimumInflow: A minimum amount that has to be paid in every month.
- accountOverdraftLimit: An overdraft is an extension of credit from a lending institution when an account reaches zero. An overdraft allows the individual to continue withdrawing money even if the account has no funds in it. Basically the bank allows people to borrow a set amount of money.
- bankAccountType: The type of a bank account.

## Usage Example

```typescript
interface BankAccount {
  $context?: 'schema.org'
  $type: 'BankAccount'
  accountMinimumInflow?: MonetaryAmount
  accountOverdraftLimit?: MonetaryAmount
  bankAccountType?: string
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "BankAccount",
  "name": "Example BankAccount",
  "description": "An example of the BankAccount type",
  "url": "https://example.com/bankaccount",
  "identifier": "example-bankaccount-1"
}
```

## Notes

- BankAccount inherits all properties from FinancialProduct
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
