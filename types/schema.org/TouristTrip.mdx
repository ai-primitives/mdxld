---
$context: schema.org
$type: TouristTrip
title: TouristTrip
description: A tourist trip. A created itinerary of visits to one or more places of interest ([[TouristAttraction]]/[[TouristDestination]]) often linked by a similar theme, geographic area, or interest to a particular [[touristType]]. The [UNWTO](http://www2.unwto.org/) defines tourism trip as the Trip taken by visitors.
  (See examples below.)
properties:

subClasses:

parentClass: Trip
---

# TouristTrip

A tourist trip. A created itinerary of visits to one or more places of interest ([[TouristAttraction]]/[[TouristDestination]]) often linked by a similar theme, geographic area, or interest to a particular [[touristType]]. The [UNWTO](http://www2.unwto.org/) defines tourism trip as the Trip taken by visitors.
  (See examples below.)

## Type Hierarchy

TouristTrip is a type of Trip.

### Direct Subtypes

No types directly inherit from TouristTrip.

## Properties

TouristTrip defines the following properties in addition to those inherited from Trip:



## Usage Example

```typescript
interface TouristTrip {
  $context?: 'schema.org'
  $type: 'TouristTrip'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "TouristTrip",
  "name": "Example TouristTrip",
  "description": "An example of the TouristTrip type",
  "url": "https://example.com/touristtrip",
  "identifier": "example-touristtrip-1"
}
```

## Notes

- TouristTrip inherits all properties from Trip
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
