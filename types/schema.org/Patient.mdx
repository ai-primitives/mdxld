---
$context: schema.org
$type: Patient
title: Patient
description: A patient is any person recipient of health care services.
properties:

subClasses:


---

# Patient

A patient is any person recipient of health care services.

## Type Hierarchy

Patient is the root type in the Schema.org hierarchy. All other types inherit from Thing either directly or indirectly.

### Direct Subtypes

No types directly inherit from Patient.

## Properties

Patient defines the following properties in addition to those inherited from Thing:



## Usage Example

```typescript
interface Patient {
  $context?: 'schema.org'
  $type: 'Patient'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "Patient",
  "name": "Example Patient",
  "description": "An example of the Patient type",
  "url": "https://example.com/patient",
  "identifier": "example-patient-1"
}
```

## Notes

- Patient inherits all properties from Thing
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
