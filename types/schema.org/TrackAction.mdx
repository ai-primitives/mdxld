---
$context: schema.org
$type: TrackAction
title: TrackAction
description: An agent tracks an object for updates.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, TrackAction refers to the interest on the location of innanimates objects.\n* [[SubscribeAction]]: Unlike SubscribeAction, TrackAction refers to  the interest on the location of innanimate objects.
properties:

subClasses:

parentClass: FindAction
---

# TrackAction

An agent tracks an object for updates.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, TrackAction refers to the interest on the location of innanimates objects.\n* [[SubscribeAction]]: Unlike SubscribeAction, TrackAction refers to  the interest on the location of innanimate objects.

## Type Hierarchy

TrackAction is a type of FindAction.

### Direct Subtypes

No types directly inherit from TrackAction.

## Properties

TrackAction defines the following properties in addition to those inherited from FindAction:



## Usage Example

```typescript
interface TrackAction {
  $context?: 'schema.org'
  $type: 'TrackAction'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "TrackAction",
  "name": "Example TrackAction",
  "description": "An example of the TrackAction type",
  "url": "https://example.com/trackaction",
  "identifier": "example-trackaction-1"
}
```

## Notes

- TrackAction inherits all properties from FindAction
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
