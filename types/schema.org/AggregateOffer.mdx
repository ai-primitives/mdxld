---
$context: schema.org
$type: AggregateOffer
title: AggregateOffer
description: When a single product is associated with multiple offers (for example, the same pair of shoes is offered by different merchants), then AggregateOffer can be used.\n\nNote: AggregateOffers are normally expected to associate multiple offers that all share the same defined [[businessFunction]] value, or default to http://purl.org/goodrelations/v1#Sell if businessFunction is not explicitly defined.
properties:
  - highPrice
  - lowPrice
  - offerCount
subClasses:

parentClass: Offer
---

# AggregateOffer

When a single product is associated with multiple offers (for example, the same pair of shoes is offered by different merchants), then AggregateOffer can be used.\n\nNote: AggregateOffers are normally expected to associate multiple offers that all share the same defined [[businessFunction]] value, or default to http://purl.org/goodrelations/v1#Sell if businessFunction is not explicitly defined.

## Type Hierarchy

AggregateOffer is a type of Offer.

### Direct Subtypes

No types directly inherit from AggregateOffer.

## Properties

AggregateOffer defines the following properties in addition to those inherited from Offer:

- highPrice: The highest price of all offers available.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator.
- lowPrice: The lowest price of all offers available.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator.
- offerCount: The number of offers for the product.

## Usage Example

```typescript
interface AggregateOffer {
  $context?: 'schema.org'
  $type: 'AggregateOffer'
  highPrice?: number | string
  lowPrice?: number | string
  offerCount?: number
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "AggregateOffer",
  "name": "Example AggregateOffer",
  "description": "An example of the AggregateOffer type",
  "url": "https://example.com/aggregateoffer",
  "identifier": "example-aggregateoffer-1"
}
```

## Notes

- AggregateOffer inherits all properties from Offer
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
