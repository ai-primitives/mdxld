---
$context: schema.org
$type: UpdateAction
title: UpdateAction
description: The act of managing by changing/editing the state of the object.
properties:
  - collection
  - targetCollection
subClasses:
  - AddAction
  - DeleteAction
  - ReplaceAction
parentClass: Action
---

# UpdateAction

The act of managing by changing/editing the state of the object.

## Type Hierarchy

UpdateAction is a type of Action.

### Direct Subtypes

The following types directly inherit from UpdateAction:

- AddAction: The act of editing by adding an object to a collection.
- DeleteAction: The act of editing a recipient by removing one of its objects.
- ReplaceAction: The act of editing a recipient by replacing an old object with a new object.

## Properties

UpdateAction defines the following properties in addition to those inherited from Action:

- collection: A sub property of object. The collection target of the action.
- targetCollection: A sub property of object. The collection target of the action.

## Usage Example

```typescript
interface UpdateAction {
  $context?: 'schema.org'
  $type: 'UpdateAction'
  collection?: Thing
  targetCollection?: Thing
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "UpdateAction",
  "name": "Example UpdateAction",
  "description": "An example of the UpdateAction type",
  "url": "https://example.com/updateaction",
  "identifier": "example-updateaction-1"
}
```

## Notes

- UpdateAction inherits all properties from Action
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
