---
$context: schema.org
$type: CreativeWorkSeason
title: CreativeWorkSeason
description: A media season, e.g. TV, radio, video game etc.
properties:
  - seasonNumber
subClasses:
  - PodcastSeason
  - RadioSeason
parentClass: CreativeWork
---

# CreativeWorkSeason

A media season, e.g. TV, radio, video game etc.

## Type Hierarchy

CreativeWorkSeason is a type of CreativeWork.

### Direct Subtypes

The following types directly inherit from CreativeWorkSeason:

- PodcastSeason: A single season of a podcast. Many podcasts do not break down into separate seasons. In that case, PodcastSeries should be used.
- RadioSeason: Season dedicated to radio broadcast and associated online delivery.

## Properties

CreativeWorkSeason defines the following properties in addition to those inherited from CreativeWork:

- seasonNumber: Position of the season within an ordered group of seasons.

## Usage Example

```typescript
interface CreativeWorkSeason {
  $context?: 'schema.org'
  $type: 'CreativeWorkSeason'
  seasonNumber?: number | string
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "CreativeWorkSeason",
  "name": "Example CreativeWorkSeason",
  "description": "An example of the CreativeWorkSeason type",
  "url": "https://example.com/creativeworkseason",
  "identifier": "example-creativeworkseason-1"
}
```

## Notes

- CreativeWorkSeason inherits all properties from CreativeWork
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
