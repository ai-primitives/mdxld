---
$context: schema.org
$type: LiquorStore
title: LiquorStore
description: A shop that sells alcoholic drinks such as wine, beer, whisky and other spirits.
properties:

subClasses:

parentClass: Store
---

# LiquorStore

A shop that sells alcoholic drinks such as wine, beer, whisky and other spirits.

## Type Hierarchy

LiquorStore is a type of Store.

### Direct Subtypes

No types directly inherit from LiquorStore.

## Properties

LiquorStore defines the following properties in addition to those inherited from Store:



## Usage Example

```typescript
interface LiquorStore {
  $context?: 'schema.org'
  $type: 'LiquorStore'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "LiquorStore",
  "name": "Example LiquorStore",
  "description": "An example of the LiquorStore type",
  "url": "https://example.com/liquorstore",
  "identifier": "example-liquorstore-1"
}
```

## Notes

- LiquorStore inherits all properties from Store
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
