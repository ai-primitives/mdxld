---
$context: schema.org
$type: UnRegisterAction
title: UnRegisterAction
description: The act of un-registering from a service.\n\nRelated actions:\n\n* [[RegisterAction]]: antonym of UnRegisterAction.\n* [[LeaveAction]]: Unlike LeaveAction, UnRegisterAction implies that you are unregistering from a service you were previously registered, rather than leaving a team/group of people.
properties:

subClasses:

parentClass: InteractAction
---

# UnRegisterAction

The act of un-registering from a service.\n\nRelated actions:\n\n* [[RegisterAction]]: antonym of UnRegisterAction.\n* [[LeaveAction]]: Unlike LeaveAction, UnRegisterAction implies that you are unregistering from a service you were previously registered, rather than leaving a team/group of people.

## Type Hierarchy

UnRegisterAction is a type of InteractAction.

### Direct Subtypes

No types directly inherit from UnRegisterAction.

## Properties

UnRegisterAction defines the following properties in addition to those inherited from InteractAction:



## Usage Example

```typescript
interface UnRegisterAction {
  $context?: 'schema.org'
  $type: 'UnRegisterAction'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "UnRegisterAction",
  "name": "Example UnRegisterAction",
  "description": "An example of the UnRegisterAction type",
  "url": "https://example.com/unregisteraction",
  "identifier": "example-unregisteraction-1"
}
```

## Notes

- UnRegisterAction inherits all properties from InteractAction
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
