---
$context: schema.org
$type: GovernmentService
title: GovernmentService
description: A service provided by a government organization, e.g. food stamps, veterans benefits, etc.
properties:
  - serviceOperator
subClasses:

parentClass: Service
---

# GovernmentService

A service provided by a government organization, e.g. food stamps, veterans benefits, etc.

## Type Hierarchy

GovernmentService is a type of Service.

### Direct Subtypes

No types directly inherit from GovernmentService.

## Properties

GovernmentService defines the following properties in addition to those inherited from Service:

- serviceOperator: The operating organization, if different from the provider.  This enables the representation of services that are provided by an organization, but operated by another organization like a subcontractor.

## Usage Example

```typescript
interface GovernmentService {
  $context?: 'schema.org'
  $type: 'GovernmentService'
  serviceOperator?: Organization
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "GovernmentService",
  "name": "Example GovernmentService",
  "description": "An example of the GovernmentService type",
  "url": "https://example.com/governmentservice",
  "identifier": "example-governmentservice-1"
}
```

## Notes

- GovernmentService inherits all properties from Service
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
