---
$context: schema.org
$type: TelevisionStation
title: TelevisionStation
description: A television station.
properties:

subClasses:

parentClass: LocalBusiness
---

# TelevisionStation

A television station.

## Type Hierarchy

TelevisionStation is a type of LocalBusiness.

### Direct Subtypes

No types directly inherit from TelevisionStation.

## Properties

TelevisionStation defines the following properties in addition to those inherited from LocalBusiness:



## Usage Example

```typescript
interface TelevisionStation {
  $context?: 'schema.org'
  $type: 'TelevisionStation'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "TelevisionStation",
  "name": "Example TelevisionStation",
  "description": "An example of the TelevisionStation type",
  "url": "https://example.com/televisionstation",
  "identifier": "example-televisionstation-1"
}
```

## Notes

- TelevisionStation inherits all properties from LocalBusiness
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
