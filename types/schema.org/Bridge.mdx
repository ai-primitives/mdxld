---
$context: schema.org
$type: Bridge
title: Bridge
description: A bridge.
properties:

subClasses:

parentClass: CivicStructure
---

# Bridge

A bridge.

## Type Hierarchy

Bridge is a type of CivicStructure.

### Direct Subtypes

No types directly inherit from Bridge.

## Properties

Bridge defines the following properties in addition to those inherited from CivicStructure:



## Usage Example

```typescript
interface Bridge {
  $context?: 'schema.org'
  $type: 'Bridge'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "Bridge",
  "name": "Example Bridge",
  "description": "An example of the Bridge type",
  "url": "https://example.com/bridge",
  "identifier": "example-bridge-1"
}
```

## Notes

- Bridge inherits all properties from CivicStructure
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
