---
$context: schema.org
$type: HealthTopicContent
title: HealthTopicContent
description: [[HealthTopicContent]] is [[WebContent]] that is about some aspect of a health topic, e.g. a condition, its symptoms or treatments. Such content may be comprised of several parts or sections and use different types of media. Multiple instances of [[WebContent]] (and hence [[HealthTopicContent]]) can be related using [[hasPart]] / [[isPartOf]] where there is some kind of content hierarchy, and their content described with [[about]] and [[mentions]] e.g. building upon the existing [[MedicalCondition]] vocabulary.
  
properties:
  - hasHealthAspect
subClasses:

parentClass: WebContent
---

# HealthTopicContent

[[HealthTopicContent]] is [[WebContent]] that is about some aspect of a health topic, e.g. a condition, its symptoms or treatments. Such content may be comprised of several parts or sections and use different types of media. Multiple instances of [[WebContent]] (and hence [[HealthTopicContent]]) can be related using [[hasPart]] / [[isPartOf]] where there is some kind of content hierarchy, and their content described with [[about]] and [[mentions]] e.g. building upon the existing [[MedicalCondition]] vocabulary.
  

## Type Hierarchy

HealthTopicContent is a type of WebContent.

### Direct Subtypes

No types directly inherit from HealthTopicContent.

## Properties

HealthTopicContent defines the following properties in addition to those inherited from WebContent:

- hasHealthAspect: Indicates the aspect or aspects specifically addressed in some [[HealthTopicContent]]. For example, that the content is an overview, or that it talks about treatment, self-care, treatments or their side-effects.

## Usage Example

```typescript
interface HealthTopicContent {
  $context?: 'schema.org'
  $type: 'HealthTopicContent'
  hasHealthAspect?: HealthAspectEnumeration
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "HealthTopicContent",
  "name": "Example HealthTopicContent",
  "description": "An example of the HealthTopicContent type",
  "url": "https://example.com/healthtopiccontent",
  "identifier": "example-healthtopiccontent-1"
}
```

## Notes

- HealthTopicContent inherits all properties from WebContent
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
