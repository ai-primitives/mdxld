---
$context: schema.org
$type: MusicPlaylist
title: MusicPlaylist
description: A collection of music tracks in playlist form.
properties:
  - numTracks
subClasses:
  - MusicAlbum
  - MusicRelease
parentClass: CreativeWork
---

# MusicPlaylist

A collection of music tracks in playlist form.

## Type Hierarchy

MusicPlaylist is a type of CreativeWork.

### Direct Subtypes

The following types directly inherit from MusicPlaylist:

- MusicAlbum: A collection of music tracks.
- MusicRelease: A MusicRelease is a specific release of a music album.

## Properties

MusicPlaylist defines the following properties in addition to those inherited from CreativeWork:

- numTracks: The number of tracks in this album or playlist.

## Usage Example

```typescript
interface MusicPlaylist {
  $context?: 'schema.org'
  $type: 'MusicPlaylist'
  numTracks?: number
}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "MusicPlaylist",
  "name": "Example MusicPlaylist",
  "description": "An example of the MusicPlaylist type",
  "url": "https://example.com/musicplaylist",
  "identifier": "example-musicplaylist-1"
}
```

## Notes

- MusicPlaylist inherits all properties from CreativeWork
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
