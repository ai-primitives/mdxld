---
$context: schema.org
$type: RoofingContractor
title: RoofingContractor
description: A roofing contractor.
properties:

subClasses:

parentClass: HomeAndConstructionBusiness
---

# RoofingContractor

A roofing contractor.

## Type Hierarchy

RoofingContractor is a type of HomeAndConstructionBusiness.

### Direct Subtypes

No types directly inherit from RoofingContractor.

## Properties

RoofingContractor defines the following properties in addition to those inherited from HomeAndConstructionBusiness:



## Usage Example

```typescript
interface RoofingContractor {
  $context?: 'schema.org'
  $type: 'RoofingContractor'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "RoofingContractor",
  "name": "Example RoofingContractor",
  "description": "An example of the RoofingContractor type",
  "url": "https://example.com/roofingcontractor",
  "identifier": "example-roofingcontractor-1"
}
```

## Notes

- RoofingContractor inherits all properties from HomeAndConstructionBusiness
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
