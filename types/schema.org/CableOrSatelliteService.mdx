---
$context: schema.org
$type: CableOrSatelliteService
title: CableOrSatelliteService
description: A service which provides access to media programming like TV or radio. Access may be via cable or satellite.
properties:

subClasses:

parentClass: Service
---

# CableOrSatelliteService

A service which provides access to media programming like TV or radio. Access may be via cable or satellite.

## Type Hierarchy

CableOrSatelliteService is a type of Service.

### Direct Subtypes

No types directly inherit from CableOrSatelliteService.

## Properties

CableOrSatelliteService defines the following properties in addition to those inherited from Service:



## Usage Example

```typescript
interface CableOrSatelliteService {
  $context?: 'schema.org'
  $type: 'CableOrSatelliteService'

}
```

## Example Usage

```json
{
  "$context": "schema.org",
  "$type": "CableOrSatelliteService",
  "name": "Example CableOrSatelliteService",
  "description": "An example of the CableOrSatelliteService type",
  "url": "https://example.com/cableorsatelliteservice",
  "identifier": "example-cableorsatelliteservice-1"
}
```

## Notes

- CableOrSatelliteService inherits all properties from Service
- Consider using more specific subtypes if available
- Ensure all required properties are provided
- Use appropriate property types as defined in the interface
